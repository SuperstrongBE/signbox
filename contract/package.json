{
  "name": "signbox-contract",
  "version": "0.1.0",
  "description": "SignBox on-chain policy contract (XPR / AssemblyScript)",
  "private": true,
  "license": "UNLICENSED",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "build": "npx proton-asc assembly/signbox.contract.ts",
    "test": "mocha -s 250 tests/*.spec.ts -r ts-node/register --timeout 20000"
  },
  "dependencies": {
    "proton-tsc": "^0.3.58"
  },
  "devDependencies": {
    "@greymass/eosio": "^0.5.5",
    "@proton/vert": "^0.3.18",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.1.0",
    "@types/node": "^17.0.22",
    "chai": "^4.3.6",
    "mocha": "^9.2.2",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.2"
  },
  "overrides": {
    "typescript": "^4.6.2"
  }
}
